const appPrefix = '';

const routePath = {
  login: `${appPrefix}/login`,
  register: `${appPrefix}/register`,
  dashboard: `${appPrefix}/dashboard`,
  daily: `${appPrefix}/daily`,
  dailyInfo: `${appPrefix}/daily-info`,
};

export default routePath;
